- name: configure frontend
  hosts: frontend
  become: yes
  tasks:
    - name: enable nginx
      ansible.builtin.command: dnf module enable nginx:1.24 -y

    - name: installing nodejs
      ansible.builtin.dnf:
        name: nginx
        state: present